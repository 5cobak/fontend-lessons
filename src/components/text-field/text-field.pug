mixin text-field({ title, type, name, placeholder, required, pat })
  label.text-field
    if title
      span.text-field__title #{title}
    input.text-field__input(
      type=type 
      name=name
      placeholder=placeholder
      pattern=pat 
      required=required
    )


