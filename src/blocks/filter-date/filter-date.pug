mixin filter-date({className, title})
  .filter-date
    label
      span.filter-date__title #{title}
      input(type='text' placeholder="от и до" class=className readonly).filter-date__input#js-filter-date
